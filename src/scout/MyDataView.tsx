// src/scout/MyDataView.tsx — STUB (implemented by Workstream B).
// Lists the matches scouted by THIS device's selected scouter.
export default function MyDataView(): JSX.Element {
  return (
    <div data-testid="my-data" className="flex min-h-screen flex-col gap-4 bg-background p-4 text-foreground">
      <h1 className="text-2xl font-bold">My Data</h1>
      <p className="text-muted-foreground">No data yet.</p>
    </div>
  );
}
